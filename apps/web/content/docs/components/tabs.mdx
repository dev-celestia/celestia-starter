---
title: "Tabs"
description: "Switch between views in the same context, built on the Base UI `Tabs` primitive."
---

Switch between views in the same context, built on the Base UI `Tabs` primitive.

<TabsPreview />

```tsx
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@celestia-project/ui/components/tabs"

<Tabs defaultValue="account" className="w-80">
  <TabsList>
    <TabsTrigger value="account">Account</TabsTrigger>
    <TabsTrigger value="password">Password</TabsTrigger>
  </TabsList>
  <TabsContent value="account">Make changes to your account here.</TabsContent>
  <TabsContent value="password">Change your password here.</TabsContent>
</Tabs>
```

`TabsList` accepts `variant="default" | "line"`.
