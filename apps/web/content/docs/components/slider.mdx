---
title: "Slider"
description: "A draggable range input, built on the Base UI `Slider` primitive. Supports single and range values."
---

A draggable range input, built on the Base UI `Slider` primitive. Supports single and range values.

<SliderPreview />

```tsx
import { Slider } from "@celestia-project/ui/components/slider"

<Slider defaultValue={[50]} max={100} step={1} className="w-64" />
```
