---
title: "Tooltip"
description: "A floating label shown on hover/focus, built on the Base UI `Tooltip` primitive."
---

A floating label shown on hover/focus, built on the Base UI `Tooltip` primitive.

<TooltipPreview />

```tsx
import {
  Tooltip,
  TooltipContent,
  TooltipProvider,
  TooltipTrigger,
} from "@celestia-project/ui/components/tooltip"
import { Button } from "@celestia-project/ui/components/button"

<TooltipProvider>
  <Tooltip>
    <TooltipTrigger render={<Button variant="outline" />}>Hover me</TooltipTrigger>
    <TooltipContent>Add to library</TooltipContent>
  </Tooltip>
</TooltipProvider>
```

Wrap your app (or the relevant subtree) in a single `TooltipProvider`.
