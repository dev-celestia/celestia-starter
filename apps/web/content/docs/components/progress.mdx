---
title: "Progress"
description: "Displays the completion status of a task, built on the Base UI `Progress` primitive."
---

Displays the completion status of a task, built on the Base UI `Progress` primitive.

<ProgressPreview />

```tsx
import { Progress } from "@celestia-project/ui/components/progress"

<Progress value={60} className="w-64" />
```

`ProgressLabel` and `ProgressValue` render the accessible label and current value.
