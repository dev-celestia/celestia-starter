---
title: "Badge"
description: "A small status descriptor, rendered with the Base UI `useRender` API so it can be any element via `render`."
---

A small status descriptor, rendered with the Base UI `useRender` API so it can be any element via `render`.

<BadgePreview />

```tsx
import { Badge } from "@celestia-project/ui/components/badge"

<Badge>Default</Badge>
<Badge variant="secondary">Secondary</Badge>
<Badge variant="destructive">Destructive</Badge>
<Badge variant="outline">Outline</Badge>
```

| Prop | Type | Default |
|------|------|---------|
| `variant` | `default \| secondary \| destructive \| outline \| ghost \| link` | `default` |
